

# auth endpoint: redirect to Oauth provider for login/authenticate
	
# Oauth provider call your callback endpoint and give you the oauth token
	
# use oauth token to get user info from Oauth provider
	
# validate if the request user match the info from Oauth provider

class Auth():
    def __init__(self):
        return
